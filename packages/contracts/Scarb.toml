[package]
name = "tongo"
version = "0.1.0"
edition = "2023_11"

# See more keys and their definitions at https://docs.swmansion.com/scarb/docs/reference/manifest.html

[dependencies]
starknet = "2.12.2"
she = "0.4.0"

[dev-dependencies]
snforge_std = "0.50.0"

[[target.starknet-contract]]
sierra = true
casm = true

[scripts]
test = "snforge test"
format = "cairo-format --merge-use-items=true --sort-mod-level-items=true -r src"

# Needed for compilation to declare and deploy with sncast scripts
[lib]
sierra = true
casm = true

